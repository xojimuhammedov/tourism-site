import { Box } from '@chakra-ui/react';
import React from 'react';

const Navbar = () => {
    return (
        <Box {...css.navbar}>
            <Box className='container'>
                
            </Box>
        </Box>
    );
}

export default Navbar;

const css = {
    navbar: {
        padding: "12px 0"
    }
}